apply plugin: "war"

bootWar {
    mainClassName = "com.vdharam.store.InvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
